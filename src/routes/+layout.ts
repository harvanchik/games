// GitHub Pages serves built files only, so generate every route at build time.
export const prerender = true;

// Directory-style pages keep nested game routes usable on static hosts.
export const trailingSlash = 'always';
